{
  "name": "pulse-ai",
  "version": "1.0.0",
  "description": "Pulse AI - Autonomous Academic Workload Density Auditor & Task Manager",
  "private": true,
  "scripts": {
    "test": "node --test server/tests/agentCore.test.js client/src/utils/schedulerEngine.test.js",
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
